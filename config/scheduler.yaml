# Task Scheduler Configuration
# Supports: hourly, daily, weekly, or custom intervals like "every_30_minutes"

tasks:
  # Twitter auto-tweet (if enabled in twitter.yaml)
  - name: "twitter_auto_tweet"
    type: "twitter"
    interval: "daily"
    enabled: true

  # Memory reflection (analyze conversations periodically)
  - name: "memory_reflection"
    type: "memory_reflection"
    interval: "hourly"
    enabled: true

  # Custom task example
  # - name: "custom_task"
  #   type: "custom"
  #   interval: "every_30_minutes"
  #   enabled: false
  #   command: "echo 'Hello from scheduler'"

  # Generic workflow example:
  # Fetches JSON, evaluates a condition expression, then runs chained actions.
  # - name: "sample_workflow"
  #   type: "workflow"
  #   interval: "every_5_minutes"
  #   enabled: true
  #   source_url: "https://api.coingecko.com/api/v3/simple/price?ids=bitcoin&vs_currencies=usd"
  #   steps:
  #     - action: "set"
  #       key: "price"
  #       expr: "float(source['bitcoin']['usd'])"
  #     - action: "notify"
  #       when: "state.get('last_price') and ((price - float(state['last_price'])) / float(state['last_price']) * 100) >= 2"
  #       message: "BTC moved up >=2%. price={price} at {now_utc}"
  #     - action: "set_state"
  #       key: "last_price"
  #       expr: "price"
  #   notify_user_id: 123456789
